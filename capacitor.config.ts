import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.radej.mobiledon',
  appName: 'Mobiledon',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
